export class RectangleAnnotation {
    constructor(
        state,
        uuid,
        left,
        top,
        width,
        height,
        stroke,
        strokeUniform,
        strokeWidth,
        angle,
        fill,
        initialWidth,
        page,
        
    ) {
    }
}